# Budget Assistant Environment Configuration
# Copy this file to .env and update with your local values

# Optional: Set the data directory path (defaults to ./data)
# DATA_DIR=./data

# Optional: Set the output directory path (defaults to ./output)
# OUTPUT_DIR=./output

# Optional: Enable debug logging
# DEBUG=false
